#include <math.h>
#include <stdio.h>
#include <stdlib.h>

/// structure that describes a point
struct point {
    int x;
    int y;
};

typedef struct point point_t;

struct point struct_init();
int pointSum(struct point, point_t);

// pointers

point_t* struct_init2();
void struct_free(point_t *p);


typedef int I_love_programming;


